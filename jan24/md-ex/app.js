const fileReader = require('./file/reader');
const fileWriter = require('./file/writer');

const models = require('./models');

console.log(models);

// console.log(fileReader);
// fileReader.readTxt();

// const p = new models.product.Product;
// if (some > models.productGroup.X) {

// }